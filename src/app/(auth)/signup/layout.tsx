export const metadata = {
  title: "Sign Up - CineScore",
  description: "Sign up for a CineScore account",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
