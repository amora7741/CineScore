export const metadata = {
  title: "Log In - CineScore",
  description: "Log in to your CineScore account",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
